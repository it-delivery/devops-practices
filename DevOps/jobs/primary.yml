jobs:
  - deployment: 'core'
    displayName: "${{ format ('Deploy {0} stage',  'Primary') }}"
    pool:
      name: Default
      demands:
        - Agent.OS -equals Linux
    environment: "Primary"
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: self